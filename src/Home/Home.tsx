import * as React from 'react';

const Home: React.StatelessComponent<{}> = (props: {}) => {
  return (
    <p>
      Listing of tools with descriptions will go here. Blah blah blah. Navigation is only above for now.
    </p>
  );
};

export default Home;
